---
interface Props {
  class?: string;
  variant?: 'default' | 'light' | 'dark'; // Kept for API compatibility
  loading?: 'eager' | 'lazy';
}

const { 
  class: className,
  loading = 'eager'
} = Astro.props;
---

<a href="/" class:list={["block relative transition-opacity hover:opacity-90", className]} aria-label="Blue Lawns Home">
  <img 
    src="/logo/logo.png" 
    alt="Blue Lawns – Coastal Lawn Care & Landscaping" 
    class="h-auto w-full object-contain block"
    width="160"
    height="45"
    loading={loading}
  />
</a>
